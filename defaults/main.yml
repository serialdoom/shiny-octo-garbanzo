---
# defaults file for shiny-octo-garbanzo/
downloads: /vagrant/downloads
user: vagrant
restart: false
pia_username: "{{ lookup('env','PIA_USERNAME') }}"
pia_password: "{{ lookup('env','PIA_PASSWORD') }}"
external_ip: "{{ ansible_eth1.ipv4.address }}"
pia_profile: Italy.ovpn
squid: false
